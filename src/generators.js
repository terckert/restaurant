let defineH1 = (
    id,         // ID of the h1 element
    text        // Inner text of the h1 element
    ) => {
        hOne = document.createElement('h1')
        hOne.id = id
        hOne.innerText = text
        return hOne
    }







